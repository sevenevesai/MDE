name: Guard test

# TEMPORARY — deleted right after one dispatch. Simulates a workflow reaching
# the self-hosted runner via a non-release trigger; the runner-side job guard
# (job-guard.ps1, ACTIONS_RUNNER_HOOK_JOB_STARTED) must fail this job before
# its first step.

on: workflow_dispatch

jobs:
  probe:
    runs-on: [self-hosted, mde-windows]
    steps:
      - name: Should never run
        shell: powershell
        run: Write-Output "GUARD DID NOT BLOCK THIS JOB"
